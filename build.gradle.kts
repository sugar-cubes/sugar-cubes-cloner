apply(from = "licenseFormat.gradle.kts")

allprojects {
    group = "org.sugarcubes"
    version = "1.0.3"
    description = "Java Reflection Cloner library"

    repositories {
        mavenCentral()
    }
}
